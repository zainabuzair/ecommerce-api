E-Commerce REST API 🛒A robust, production-ready REST API built with Laravel 11, focusing on secure authentication, role-based access control, and a complete shopping workflow.🚀 Core FeaturesAuthentication: Secure user registration and login using Laravel Sanctum (token-based).Product Management: Full CRUD functionality restricted to Admin roles.Shopping Cart: Persistent cart logic for authenticated users.Order System: Seamless checkout process converting cart items to orders.Order Tracking: Dynamic order status management (Pending, Processing, Shipped, Delivered).Database Architecture: Optimized relationships between Users, Products, Carts, and Orders.🛠 Tech StackFramework: Laravel 11Database: MySQLAuth: Laravel SanctumLanguage: PHP 8.2+📊 Database SchemaThe project utilizes a relational database structure to ensure data integrity.Key Relationships:User ↔ Order: One-to-Many (A user can have multiple orders).Order ↔ Product: Many-to-Many via order_items (pivot table).User ↔ Cart: One-to-One/Many (Managing active shopping sessions).🚦 API Endpoints (Quick Reference)AuthenticationMethodEndpointDescriptionPOST/api/registerCreate a new accountPOST/api/loginGet bearer tokenProductsMethodEndpointAccessDescriptionGET/api/productsPublicList all productsPOST/api/productsAdminCreate a productPUT/api/products/{id}AdminUpdate product detailsOrders & CartMethodEndpointAccessDescriptionPOST/api/cart/addUserAdd item to cartPOST/api/ordersUserCheckout/Place orderGET/api/orders/{id}User/AdminView order status⚙️ Installation & SetupClone the repository:Bashgit clone https://github.com/yourusername/ecommerce-api.git
cd ecommerce-api
Install dependencies:Bashcomposer install
Configure Environment:Bashcp .env.example .env
# Update your DB_DATABASE, DB_USERNAME, and DB_PASSWORD in .env
Generate App Key & Migrate:Bashphp artisan key:generate
php artisan migrate --seed
Run the server:Bashphp artisan serve
🛡 Security NoteThis API uses Middleware to protect sensitive routes.auth:sanctum ensures the user is logged in.isAdmin (Custom Middleware) ensures only authorized staff can modify the product catalog.